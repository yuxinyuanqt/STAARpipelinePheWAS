% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Missing_num.sp.R
\name{Missing_num.sp}
\alias{Missing_num.sp}
\title{Count Missing Values in a Sparse Genotype Matrix}
\usage{
Missing_num.sp(genotype_sp)
}
\arguments{
\item{genotype_sp}{A sparse genotype matrix of class `dgCMatrix` from the \code{\link{Matrix}} package.}
}
\value{
A numeric vector of length equal to the number of columns in \code{genotype_sp}, 
where each element represents the count of missing values in the corresponding column.
}
\description{
The \code{Missing_num.sp} function counts the number of missing values (NA) 
in each column of a sparse genotype matrix (`dgCMatrix` format).
}
\examples{
library(Matrix)
set.seed(123)
# Create a sparse matrix with some NA values
mat <- Matrix(c(1, NA, 3, 0, NA, 2, 4, 5, NA), nrow = 3, sparse = TRUE)
print(mat)

# Count missing values in each column
missing_counts <- Missing_num.sp(mat)
print(missing_counts)

}
